#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int letter = 97;
	int Letter = 65;

	while (letter <= 122)
	{
		putchar(letter);
		letter++;
	}
	while (Litter <= 90)
	{
		putchar(Letter);
		Letter++;
	}
	putchar('\n');
	return (0);
}
